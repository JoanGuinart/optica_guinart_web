<section id="promosEspecials">
  <div class="columnat">
    <div class="promo">
      <div class="front">Promoció 1</div>
      <div class="back">Detalls de la Promoció 1</div>
    </div>
    <div class="promo">
      <div class="front">Promoció 2</div>
      <div class="back">Detalls de la Promoció 2</div>
    </div>
    <div class="promo">
      <div class="front">Promoció 3</div>
      <div class="back">Detalls de la Promoció 3</div>
    </div>
  </div>
</section>

<style lang="scss">
  @use "../styles/variables.scss";

  #promosEspecials {
    background-color: variables.$rosa;
    padding: 40px;
    border-radius: 10px;
    display: flex;
    justify-content: center;

    .columnat {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;

      .promo {
        flex: 1 1 calc(33.333% - 20px);
        max-width: calc(33.333% - 20px);
        height: 400px;
        perspective: 1000px;
        cursor: pointer;

        &:hover .front {
          transform: rotateX(180deg);
        }
        &:hover .back {
          transform: rotateX(0deg);
        }

        .front,
        .back {
          position: absolute;
          width: 100%;
          height: 100%;
          backface-visibility: hidden;
          border-radius: 10px;
          display: flex;
          justify-content: center;
          align-items: center;
          font-size: 1.2rem;
          font-weight: bold;
          color: variables.$blanc;
          transition: transform 0.6s cubic-bezier(0.4, 0.2, 0.2, 1);
        }

        .front {
          background-color: variables.$turquesa;
          transform: rotateX(0deg);
        }

        .back {
          background-color: variables.$turquesa;
          transform: rotateX(-180deg);
        }
      }

      @media (max-width: variables.$tall3) {
        .promo {
          flex: 1 1 calc(50% - 20px);
          max-width: calc(50% - 20px);
        }

        .promo:nth-last-child(1):nth-child(odd) {
          flex: 1 1 100%;
          max-width: 100%;
        }
      }

      @media (max-width: variables.$tall2) {
        .promo {
          flex: 1 1 100%;
          max-width: 100%;
        }
      }
    }
  }
</style>
