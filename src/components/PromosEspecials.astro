<section id="promosEspecials">
  <div class="columnat">
    <div class="promo">
      <div class="front">Descompte en montures de marca</div>
      <div class="back">15% descompte  en montures de marca</div>
    </div>
    <div class="promo">
      <div class="front">Descompte en audiòfons</div>
      <div class="back">40% descompte audiófons recargables</div>
    </div>
    <div class="promo">
      <div class="front">Descompte de ray-ban</div>
      <div class="back">40% descompte ray-ban graduada progressiva</div>
    </div>
  </div>
</section>

<style lang="scss">
  @use "../styles/variables.scss";

  #promosEspecials {
    background-color: variables.$gris;
    padding: 40px;
    display: flex;
    justify-content: center;

    .columnat {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;

      .promo {
        flex: 1 1 calc(33.333% - 20px);
        max-width: calc(33.333% - 20px);
        height: 400px;
        perspective: 1000px;
        cursor: pointer;

        &:hover .front {
          transform: rotateX(180deg);
        }
        &:hover .back {
          transform: rotateX(0deg);
        }

        .front,
        .back {
          position: absolute;
          width: 100%;
          height: 100%;
          backface-visibility: hidden;
          border-radius: 10px;
          display: flex;
          justify-content: center;
          align-items: center;
          font-size: 1.2rem;
          font-weight: bold;
          transition: transform 0.6s cubic-bezier(0.4, 0.2, 0.2, 1);
        }
        
        .front {
          color: variables.$carbo-fosc;
          background-color: variables.$beix;
          transform: rotateX(0deg);
          font-family: 'Playfair display', serif;
          text-transform: uppercase;
        }
        
        .back {
          color: variables.$blanc;
          background-color: variables.$daurat-fosc;
          transform: rotateX(-180deg);
          font-family: 'Montserrat', serif;
        }
      }

      @media (max-width: variables.$tall3) {
        .promo {
          flex: 1 1 calc(50% - 20px);
          max-width: calc(50% - 20px);
        }

        .promo:nth-last-child(1):nth-child(odd) {
          flex: 1 1 100%;
          max-width: 100%;
        }
      }

      @media (max-width: variables.$tall2) {
        .promo {
          flex: 1 1 100%;
          max-width: 100%;
        }
      }
    }
  }
</style>
